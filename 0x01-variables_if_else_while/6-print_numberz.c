#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - code
 *
 * Return: Always 0 (if success, then true)
 */

int main(void)
{
	int d;

	for (d = '0'; d <= '9'; d++)
	putchar(d);
	putchar('\n');
	return (0);
}
