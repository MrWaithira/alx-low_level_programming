#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - code
 *
 * Return: ALways 0 (if success, then true)
 */

int main(void)
{
	int i;

	for  (i = 0; i < 10; i++)
	printf("%d", i);
	printf("\n");
	return (0);
}
